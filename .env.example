# Example environment variables for toyMCP
# Copy this file to .env and fill in your actual values.
# .env is ignored by git.

# Server Configuration
PORT=3000

# Database Configuration
# These should match the values in docker-compose.yml for local development
DB_USER=todos_user
DB_HOST=localhost
DB_NAME=todos_db
DB_PASSWORD=todos_password
DB_PORT=5432

# Authentication
# IMPORTANT: Use a strong, unique secret in your actual .env file for production!
JWT_SECRET=YourSuperSecretKeyForDevelopment 